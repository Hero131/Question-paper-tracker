<style>
    body {
        font-family: Arial, sans-serif;
        background: linear-gradient(to top, #cbcb55, #5c797c);
    }

    table {
        width: 2000px;
        border-collapse: collapse;
        background-color: #8383e4;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }

    th, td {
        padding: 12px;
        text-align: left;
        border: 1px solid #ddd;
    }

    th {
        background-color: #f2f2f2;
        font-weight: bold;
    }

    tr:nth-child(even) {
        background-color: #f9f9f9;
    }

    input[type="text"], input[type="submit"] {
        padding: 10px;
        font-size: 16px;
        border: none;
        border-radius: 4px;
    }

    input[type="text"] {
        width: 100%;
    }

    input[type="submit"] {
        background-color: #605999;
        color: #004cfd;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    input[type="submit"]:hover {
        background-color: #cbcb55;
    }

    input[type="submit"]:active {
        background-color: #cbcb55;
    }
</style>
<table>
    <thead>
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Department</th>
        <th>Uploaded?</th>
    </tr>
    </thead>
    <tbody>
    {{#each data}}
        <tr>
            <td>{{this.name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.department}}</td>
            <td>
                {{#if (eq this.name "Helo12")}}
                    Yes
                {{else}}
                    No
                {{/if}}
            </td>
        </tr>
    {{/each}}
    </tbody>
</table>